# 设置超时时间（单位：秒）
timeout = 120

# 开启自动重载（仅适用于开发环境）
reload = True

# 绑定地址和端口
bind = "0.0.0.0:8080"

# 日志配置
# accesslog = "-"  # 访问日志输出到标准输出
errorlog = "-"   # 错误日志输出到标准输出
# loglevel = "debug"  # 日志级别设置为 debug
